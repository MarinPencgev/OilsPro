
@{
    ViewData["Title"] = "TestAjax";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>TestAjax</h1>

<form id="myForm">
    
    <label for="receiversAddreses">Receivers</label>
    
    <select class="form-control" id="receiversAddresses" name="receiversAddresses"></select>

</form>

<script>
    $(document).ready(function() {
        $.ajax({
            type: "GET",
            url: "/Receivers/GetAddresses",
            data: "{}",
            success: function(data) {
                var options = '';
                options += '<option value="Select">Select</option>';
                for (var i = 0; i < data.length; i++) {
                    options += '<option value="' + response[i] + '">' + response[i] + '</option>';
                }
                $("#receiversAddresses").html(options);
            }
        });
    })

</script>